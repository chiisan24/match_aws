# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
*.local

# Vercel
.vercel/

# Env (never commit real AWS connection info)
.env
.env.local
.env.*.local

# Test / coverage
coverage/

# 検証・調査で吐く一時ファイル。`*.log` だけでは拾えず、実際に probe.txt や
# devlog.txt が誤ってコミットされたので明示的に除外する。
tmp-*
*.vitest.json
boot.txt
devlog.txt
probe.txt
lookup.txt

# Editor / OS
.DS_Store
*.log
.vscode/*
!.vscode/extensions.json
.idea/
